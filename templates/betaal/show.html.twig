{% extends 'base.html.twig' %}

{% block title %}Betaal{% endblock %}

{% block body %}
    <h1>Betaal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ betaal.id }}</td>
            </tr>
            <tr>
                <th>Datum</th>
                <td>{{ betaal.datum ? betaal.datum|date('Y-m-d H:i:s T') : '' }}</td>
            </tr>
            <tr>
                <th>Bank</th>
                <td>{{ betaal.bank }}</td>
            </tr>
            <tr>
                <th>Bedrag</th>
                <td>{{ betaal.bedrag }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('betaal_index') }}">back to list</a>

    <a href="{{ path('betaal_edit', {'id': betaal.id}) }}">edit</a>

    {{ include('betaal/_delete_form.html.twig') }}
{% endblock %}
